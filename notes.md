region -> some availability zones

availability zone -> some data center, fault probe, low-latency

edge location used as CDN (content delivery network) specialized CloudFront & Route 53 (dns), cached

